{
  "name": "hyperplot-desktop",
  "version": "1.0.0",
  "description": "Electron desktop interface for HyperPlot",
  "main": "electron/main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "check": "node --check electron/main.js && node --check electron/preload.js && node --check electron/renderer/app.js",
    "test": "npm run check && python3 -m unittest discover -s tests"
  },
  "devDependencies": {
    "electron": "^43.2.0"
  }
}
